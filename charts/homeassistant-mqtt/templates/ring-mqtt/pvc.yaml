{{- with .Values.ringMqtt }}
{{- if .enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ring-mqtt-data
  labels:
    {{- include "ringMqtt.labels" $ | nindent 4 }}
spec:
  {{- with .persistence }}
  accessModes:
    - {{ .accessMode }}
  resources:
    requests:
      storage: {{ .size }}
  {{- end }}
{{- end }}
{{- end }}
